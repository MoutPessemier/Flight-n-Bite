<Page
    x:Class="Flight__n_Bite.Views.FoodsDrinksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Flight__n_Bite.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:vm="using:Flight__n_Bite.ViewModels"
    xmlns:models="using:Flight__n_Bite.Models"
    xmlns:util="using:Flight__n_Bite.Util"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:ProductViewModel x:Name="productVM"/>
    </Page.DataContext>

    <Page.Resources>
        <util:IntegerToCurrencyConverter x:Key="IntegerCurrencyConverter" />
    </Page.Resources>

    <Grid>
        <Pivot>
            <PivotItem Header="All Products">
                <GridView ItemsSource="{x:Bind productVM.Products}">
                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="models:Product">
                            <StackPanel Orientation="Vertical">
                                <Image></Image>
                                <TextBlock x:Name="name" FontSize="32" Text="{x:Bind Name}"/>
                                <TextBlock x:Name="description" FontSize="12" Text="{x:Bind Description}"/>
                                <TextBlock x:Name="price" FontFamily="24" Text="{x:Bind Price, Converter={StaticResource IntegerCurrencyConverter}}"/>
                            </StackPanel>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </PivotItem>
            <PivotItem Header="My orders">
                <GridView ItemsSource="{x:Bind productVM.Orders}">
                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="models:Order">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="id"></TextBlock>
                                <TextBlock x:Name="date"></TextBlock>
                                <ListView ItemsSource="{x:Bind OrderLines}">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="models:OrderLine">
                                            <StackPanel Orientation="Vertical">
                                                <TextBlock x:Name="id" Text="{x:Bind Id}"/>
                                                <TextBlock x:Name="product" Text="{Binding productVM.getProductName(ProductId)}"/>
                                                <TextBlock x:Name="amount" Text="{x:Bind Amount}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
